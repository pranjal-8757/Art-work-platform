<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
        <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md mt-5 mb-5 inline-block px-3 py-2 text-sm " href="/logout">Logout</a>
        <a href="/profile" class="mr-4 text-white">My Profile</a>
        <a href="/explore" class="mr-4 text-white">Explore</a>
    </div>  
        <div class="flex items-start gap-2">
            <div class="w-10 h-10 rounded-md overflow-hidden">
            <img class="w-full h-full object-cover" src="/images/upload/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl ml-5 mb-5"><span class="font-light">Hello</span>, <%= user.name %></h3>
        </div>
        <h5 class="mb-5 ml-5">you can create a new post</h5>
        
        <form method="post" action="/post" enctype="multipart/form-data">
        <textarea placeholder="What's on your mind?" name="content" class="p-3 ml-5 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>

        <input type="file" name="image" class="ml-5 block mt-2 text-sm">

        <input class="px-3 ml-5 w-40 py-2 bg-blue-500 block mt-2 rounded-md" type="submit" value="Create New Post">
    </form>

        <div class="posts ml-5 mt-20">
  <h3 class="text-zinc-400"><%= loggedInUser._id === user._id ? "Your Posts." : user.name + "'s Posts" %></h3>

  <div class="postcontainer mt-5">
    <% user.posts.slice().reverse().forEach(function(post) { %>
      <div class="post w-1/3 mt-5 p-4 mb-2 ml-5 border-2 border-zinc-800">
        <h4>@<%= user.username %></h4>
        <p class="text-sm tracking-tight"><%= post.content %></p>

        <% if (post.image) { %>
          <img src="/images/upload/<%= post.image %>" alt="Post Image" class="mt-2 w-full rounded-md">
        <% } %>

        <small class="mt-4 inline-block"><%= post.likes.length %> likes </small>

        <div class="btns flex mt-5">
          <a class="text-blue-500" href="/like/<%= post._id %>">
            <%= post.likes.includes(loggedInUser._id) ? "Unlike" : "Like" %>
          </a>

          <% if (loggedInUser._id === user._id) { %>
            <a class="text-zinc-200 ml-3" href="/edit/<%= post._id %>">Edit</a>
            <a class="text-red-500 ml-3" href="/delete/<%= post._id %>">Delete</a>
          <% } %>
        </div>
      </div>
    <% }) %>
  </div>
</div>
</body>
</html>