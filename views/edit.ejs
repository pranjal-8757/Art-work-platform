<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
        <div class="w-full flex justify-end">
        <a class="bg-red-500 rounded-md mt-5 mb-5 inline-block px-3 py-2 text-sm " href="/logout">Logout</a>
        </div>  
        <h5 class="mb-5 ml-5">edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind ?" name="content" class="p-3 ml-5  w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
            <input class="px-3 ml-5 w-40 py-2 bg-yellow-500 text-black block mt-2 rounded-md" type="submit" value="Update">
        </form>
        
    </div>
</body>
</html>